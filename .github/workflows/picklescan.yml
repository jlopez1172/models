on:
  pull_request:
    branches:
      - main
    paths:
      - models/**
jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v6
        with:
          python-version: '3.11'